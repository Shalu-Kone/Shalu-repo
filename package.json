{
  "name": "shalu-repo",
  "version": "1.0.0",
  "description": "Auth0 tenant management and configuration",
  "scripts": {
    "test": "jest",
    "test:sms": "jest --testPathPattern=sms-connection"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
